version: 2

sources:
  - name: raw_data
    description: Original data from nobel-prize.org
    meta:
      external_location: "read_json_auto('./raw_data/{name}.json')"
    tables:
      - name: laureates
        description: Nested struct of laureate data
      - name: prizes
        description: Nested struct of Nobel Prizes awarded
